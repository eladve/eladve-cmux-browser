# OS junk
.DS_Store
**/.DS_Store
Thumbs.db

# Editor/temp
*.swp
*~

# Local-only plugin idea backlog — never published
IDEAS.md

# CI workflow kept local (deferred — pushing it needs the gh "workflow" token scope)
.github/
